# Проблемы и противоречия (рабочий файл)

> Временный файл для отслеживания обсуждения. Решённые пункты заменяются финальными формулировками.

---

## А. Синхронизация данных — ✅ РЕШЕНО

**Финальная формулировка (записана в ARCHITECTURE.md §1):**

- Исходный файл (PDF или DOCX) **никогда** не покидает устройство пользователя. Хранится как `source/book.pdf` или `source/book.docx` в ProjectStorage.
- На сервер уходят **только**: извлечённые текстовые блоки (для ИИ-анализа и TTS) и структурные метаданные (при ручном пуше).
- Жизненный цикл: **Инициализация** → **Local-auto-save** (мгновенно, без обращения к БД) → **Manual-push** (кнопка «На сервер»).
- Термин «dual-write» удалён. Начальная запись при upload — это единоразовая инициализация, не постоянный паттерн.

---

## Б. Разрыв данных персонажей: Парсер ↔ Студия

**Проблема:** Парсер хранит персонажей локально (`structure/characters.json` — `LocalCharacter[]`), а Студия работает с серверной таблицей `book_characters`. Правки в Парсере (алиасы, пол, описание) не попадают в Студию автоматически.

**Что не определено:**
1. Когда именно локальные персонажи должны синхронизироваться в `book_characters`? При пуше «На сервер»? При переходе в Студию?
2. Обратная синхронизация: если Студия обогатила профиль (temperament, voice_config, speech_style) — должно ли это отражаться в локальном файле?
3. Формат `LocalCharacter` беднее, чем `book_characters` (нет temperament, speech_style, voice_config, color, sort_order). Как мерджить?

**Статус:** 🔴 Ожидает обсуждения

---

## В. Маршрутизация ИИ-провайдеров в Edge Functions — ✅ РЕШЕНО

**Финальная формулировка (записана в ARCHITECTURE.md §3):**

- Все edge functions должны поддерживать пользовательский роутинг (клиент передаёт `model` + `apiKey`).
- Lovable AI — высший приоритет **только для админов**.
- Для пользователей приоритет: директ-подписка → OpenRouter → DotPoint ≡ ProxyAPI.
- Фоллбек на сервере **не выполняется** — ошибка возвращается клиенту, клиент ищет аналог по пользовательским спискам.
- TTS-модели имеют отдельную логику (будет определена позже).
- **Текущий статус реализации:** `extract-characters` и `parse-book-structure` поддерживают роутинг; `segment-scene`, `profile-characters`, `detect-inline-narrations`, `generate-atmosphere-prompt` — требуют обновления.

---

## Г. Inline-нарротации: отсутствие пользовательского контроля — ✅ РЕШЕНО

**Финальная формулировка:**

- За детект inline-нарротаций отвечает **Профайлер** (роль `profiler` в `aiRoles.ts`).
- UI для просмотра/правки — в Раскадровке (Студия). Формат хранения: `segment_phrases.metadata`.
- Требуется доработка логики Профайлера в Парсере и в Студии (см. TODO.md).

---

## Д. Терминология WORKFLOW.md: «Навигатор автосинхронизирует с БД» — ✅ РЕШЕНО

**Финальная формулировка:**

- NavSidebar работает исключительно с React-стейтом и OPFS.
- Синхронизация в БД — **только** кнопка «На сервер».

---

## Е. Неопределённость формата передачи контекста Парсер → Студия

**Проблема:** Переход между модулями использует `sessionStorage` для передачи `bookId`, `chapterId`, активной вкладки. Формальный контракт отсутствует.

**Статус:** 🔵 Отложено (не критично на текущем этапе, нет сторонних посетителей)

**Важно — сохранить для анализа:**
- `localStorage` / `sessionStorage` ключи Студии (`studio_selected_scene_idx`, `studio_active_tab`, `studio_chapter`) — **не трогать**.
- Записи `user_settings` в БД (ключ `studio_session`) — **не трогать**.
- Эти данные пригодятся для анализа формата, который нужно будет воспроизвести в новом локальном хранилище.

---

## Ж. Отсутствие единого реестра AI-ролей и их привязки к Edge Functions

**Статус:** 🟡 Отложено — зависит от доработки функционала Парсера и Студии

**Суть:** Формализация маппинга роль → функция → промпт невозможна до завершения функциональной доработки пайплайна. Состав ролевых задач ещё не финализирован.

**План действий (в порядке приоритета):**

### Фаза 1 — Функционал Парсера
1. Декодирование формата книги (PDF + добавить DOC/DOCX)
2. Семантическая разбивка на сцены (Сценарист, Stage 1)
3. Инлайн-правка контента сцен (пользователь + гибридные алгоритмы)
4. Поиск персонажей (Профайлер)
5. Профайлинг персонажей (Профайлер)
6. *Будущие задачи: возможно новые ролевые модели по мере проработки*

### Фаза 2 — Сохранение и защита данных
1. Сохранение / правка / чистка результатов в локальном хранилище
2. Защита существующих результатов при изменении кода (миграции данных)
3. Структурирование локального хранилища (стандартизация форматов)
4. Синхронизация локального состояния с БД сервера (кнопка «На сервер»)

### Фаза 3 — Реестр ролей
По итогам Фаз 1–2 станет ясно: какие роли задействованы, какие edge functions им нужны, какие промпты использовать, где применяются гибридные (хардкодовые) алгоритмы вместо AI.

**Рекомендации Lovable:**
- Не блокировать разработку отсутствием полного реестра. Вместо этого — фиксировать маппинг *по мере реализации* каждой фичи в `ARCHITECTURE.md`.
- Ввести в `aiRoles.ts` поле `edgeFunctions?: string[]` для каждой роли — заполнять по мере подключения.
- Чётко разделять: что делает AI-роль vs что делает хардкодовый алгоритм (regex, heuristics). Гибридные шаги документировать отдельно.

---

## З. Баги файлового пайплайна Парсера (аудит 2026-03-17)

> Результат полного аудита логики работы с файлами, контентом, анализом и хранением.
> Статус: 🔴 В работе

### Реестр багов

| ID | Описание | Файл | Серьёзность |
|----|----------|------|-------------|
| **B1** | ~~`reloadBook` + `handleFileSelect`: после Reload код делает INSERT вместо UPDATE~~ | `useBookManager.ts` | ✅ Исправлено |
| **B2** | ~~PDF folder-ноды не помечаются `done` при свежей загрузке~~ | `useBookManager.ts` | ✅ Исправлено |
| **B3** | ~~`reloadBook` не очищает `sessionStorage` от DOCX-данных~~ | `useBookManager.ts` | ✅ Исправлено |
| **B4** | ~~DOCX-mode теряется после page refresh → «PDF не загружен»~~ | `useChapterAnalysis.ts` + `useBookManager.ts` | ✅ Исправлено |
| **B5** | ~~`mergeOutlineWithTextToc` уплощает иерархию outline~~ | `pdf-extract.ts` | ✅ Исправлено |
| **B6** | ~~`handleFileSelect` безусловный INSERT (= B1)~~ | `useBookManager.ts` | ✅ = B1 Исправлено |
| **B7** | ~~`ensurePdfLoaded` для DOCX → null~~ (связан с B4) | `useBookManager.ts` | ✅ Исправлено |
| **B8** | ~~`openSavedBook`: если PDF отсутствует на сервере → молчаливый null~~ — добавлен toast-warning | `useBookManager.ts` | ✅ Исправлено |
| **B9** | ~~Библиотека читает `toc.json` и запрашивает сервер без необходимости~~ | `useBookManager.ts` | ✅ Исправлено |
| **B10** | ~~`openSavedBook` не проверяет серверный таймстамп при ручном открытии~~ | `useBookManager.ts` | ✅ Исправлено |
| **B11** | ~~`restoreFromLocal` не читает `structure/characters.json`~~ — загрузка персонажей обеспечивается `useParserCharacters` | `useBookManager.ts` | ✅ Исправлено |
| **B12** | ~~`acceptServerVersion` не заменяет локалку серверной копией~~ | `useBookManager.ts` | ✅ Исправлено |
| **B13** | ~~`openSavedBook` делает `upsert(rangeFixes)` — побочный эффект записи при чтении~~ | `useBookManager.ts` | ✅ Исправлено |
| **B14** | ~~Хардкод `source/book.pdf` во всех хуках — DOCX-книги не находят свой исходный файл при восстановлении, загрузке на сервер и `ensurePdfLoaded`~~ | `useBookManager.ts`, `useSaveBookToProject.ts`, `useProjectStorage.ts` | ✅ Исправлено |

### Правильная логика операций (эталон)

#### 1. Просмотр библиотеки
1. Перечислить OPFS-директории (`OPFSStorage.listProjects()`)
2. Из каждой прочитать **только** `project.json` — там есть `title`, `bookId`, `updatedAt`, `language`
3. **НЕ читать** `structure/toc.json` — структура не нужна для списка
4. **НЕ запрашивать** серверный список — пользователь мог ничего не сохранять на сервер
5. Если `project.json` отсутствует или сломан — показать проект как «повреждённый» с кнопкой удаления, **НЕ ломать** весь список
6. Серверная синхронизация — **только** при явном действии пользователя или при "New Workstation Flow" (пустой OPFS)
7. Отображать: название, дату последнего изменения (`updatedAt`), язык

#### 2. Загрузка новой книги
1. Пользователь выбирает PDF/DOCX файл
2. Извлечь TOC (PDF: outline + текстовый парсинг; DOCX: heading-парсинг)
3. Создать запись в `books` (INSERT) + `book_parts` / `book_chapters` → получить `bookId`
4. Создать OPFS-проект: `project.json` с `bookId`, `fileFormat: "pdf"|"docx"`, `updatedAt`
5. Сохранить исходный файл в `source/book.pdf` (или `source/book.docx`)
6. Сохранить структуру в `structure/toc.json`
7. Upload PDF в Supabase Storage (для восстановления на другом устройстве)
8. Folder-ноды пометить `done` (у них нет контента для анализа)

#### 2-R. Перезагрузка книги (Reload)
1. Сохранить текущий `bookId`
2. Очистить `sessionStorage` (docx_chapter_texts, docx_html)
3. Удалить старые chapters/parts/scenes из БД (каскадно)
4. Удалить `structure/` и `scenes/` из OPFS
5. Выполнить шаги 2.1–2.8 из п.2, используя **существующий bookId** (UPDATE books, не INSERT)

#### 2.1. Анализ структуры (parse-book-structure)
1. Определить формат книги из `project.json.fileFormat`
2. PDF → извлечь текст страниц через `pdfRef`; DOCX → взять из `sessionStorage`
3. Отправить текст + диапазон страниц на edge function
4. Получить сцены → сохранить в `scenes/chapter_{idx}.json`
5. Обновить статус главы → `done`
6. Auto-save в OPFS (через `useImperativeSave`)

#### 2.2. Разбивка на сцены (segment-scene)
- Выполняется в Студии, не в Парсере (здесь только структурный анализ)

#### 3. Открытие книги из библиотеки
1. Метаданные (`bookId`, `fileFormat`, `title`, `updatedAt`) **уже в памяти** из п.1 — повторное чтение `project.json` не нужно
2. Запросить `books.updated_at` с сервера для данного `bookId`
3. **Если** серверный `updated_at` > локальный `updatedAt` (с допуском 2 сек) → показать диалог подтверждения «На сервере есть более новая версия. Загрузить?»
4. **Если** пользователь подтвердил → загрузить серверную копию (TOC, сцены, персонажи, метаданные) → **полностью заменить** локальный проект в OPFS → продолжить открытие
5. **Если** пользователь отказался или серверная версия НЕ новее → продолжить с локальными данными
6. Прочитать `structure/toc.json` → восстановить TOC-дерево
7. Прочитать `scenes/chapter_*.json` → восстановить результаты анализа
8. Прочитать `structure/characters.json` → восстановить персонажей
9. Прочитать исходный файл (`source/book.pdf` или `source/book.docx`)
10. **Если** исходный файл отсутствует локально → показать предупреждение, анализ невозможен

**Важно:** `updatedAt` в `project.json` обновляется только локально (при любой правке). На сервер `books.updated_at` попадает **только** по кнопке «Сохранить на сервер». Таким образом серверный таймстамп новее — это признак работы с другого устройства.

#### 4. Удаление книги
1. Удалить OPFS-директорию целиком (`OPFSStorage.deleteProject`)
2. Удалить запись из `books` (каскад удалит chapters, scenes, etc.)
3. Удалить PDF из Supabase Storage
4. Убрать `localStorage` запись (`booker_last_project`)

#### 5. Удаление секции в навигаторе
1. Удалить узел из TOC-дерева (с потомками если folder)
2. Удалить соответствующие `scenes/chapter_*.json` из OPFS
3. Пересчитать `chapter_number` для оставшихся
4. Auto-save

#### 6. Сохранение изменений
- **Автосохранение**: `useImperativeSave` пишет в OPFS при каждом значимом изменении
- **Ручной push**: кнопка «На сервер» → upsert в Supabase (books, chapters, parts, scenes, characters)
- **updatedAt**: обновляется в `project.json` при каждом автосохранении

---

### План исправлений (порядок работы)

#### Волна 1 — Критичные (блокируют пользователей)

**B5: Merge уничтожает иерархию PDF outline**
- Корень: `mergeOutlineWithTextToc` вызывает `flatten(outline)` → теряет `children`
- Исправление: вставлять новые текстовые записи в существующую иерархию, не уплощая outline
- Файл: `src/lib/pdf-extract.ts`

**B1/B6: Reload → дубликат книги**
- Корень: `handleFileSelect` всегда делает INSERT, не учитывая текущий `bookId`
- Исправление: если `bookId` уже существует → UPDATE `books` + удалить старые chapters/parts/scenes, а не создавать новую запись
- Файл: `src/hooks/useBookManager.ts`

**B4/B7: DOCX-mode теряется после page refresh**
- Корень: нет персистентного признака «книга = DOCX» (ни в локале, ни в state)
- Исправление: сохранять `fileFormat: "pdf" | "docx"` в `project.json` и `structure/toc.json`; `useChapterAnalysis` проверяет формат перед вызовом `ensurePdfLoaded`
- Файлы: `useBookManager.ts`, `useChapterAnalysis.ts`, `localSync.ts`

#### Волна 2 — Средние (UX-проблемы)

**B2: Folder-ноды остаются pending**
- Исправление: в `handleFileSelect` после загрузки TOC пометить все folder-ноды как `done`
- Файл: `src/hooks/useBookManager.ts`

**B3: sessionStorage не очищается при reload**
- Исправление: `reloadBook` должен удалять `docx_chapter_texts`, `docx_html`
- Файл: `src/hooks/useBookManager.ts`

**B8: Молчаливый null при отсутствии PDF на сервере**
- Исправление: если `pdfBlob = null` и книга НЕ DOCX → показать toast-предупреждение
- Файл: `src/hooks/useBookManager.ts`

---

### Сводка расхождений кода с эталонной логикой (п.1 и п.3)

> Результат сравнения `useBookManager.ts` с обновлённой пошаговой логикой из секции «Правильная логика операций».

| # | Расхождение | Эталон | Код (useBookManager.ts) | Серьёзность |
|---|-------------|--------|------------------------|------------|
| 1 | Библиотека читает `toc.json` | Только `project.json` | `mapLocalStructureToBook` строка ~81 читает `structure/toc.json`, вычисляет `chapterCount` | 🟡 Средне |
| 2 | Библиотека запрашивает сервер | Нет серверных запросов при просмотре | `loadLibrary` строки ~231-240: `Promise.all([loadLocalLibrary(), loadLibraryFromServer()])` | 🔴 Критично |
| 3 | `openSavedBook` не проверяет таймстамп | Проверять `books.updated_at` при каждом открытии | строки ~490-496: `restoreFromLocal` → успех → return, без `checkServerNewer` | 🔴 Критично |
| 4 | `restoreFromLocal` не грузит персонажей | Читать `structure/characters.json` | строки ~277-342: восстанавливает TOC, сцены, parts, но не characters | 🟡 Средне |
| 5 | `acceptServerVersion` не заменяет локалку | Полная замена OPFS серверной копией | строки ~409-417: вызывает `openSavedBook`, который при наличии локалки снова возвращает `restoreFromLocal` → true → локалка НЕ заменяется | 🔴 Критично |
| 6 | Upsert page ranges при чтении | Не писать в БД при открытии книги | строки ~607-627: `supabase.from('book_chapters').upsert(rangeFixes)` — побочный эффект записи при операции чтения | 🟡 Средне |

**Связь с реестром багов:**
- Расхождение #1 → B9 (библиотека читает лишние файлы)
- Расхождение #2 → B9 (серверный запрос без необходимости)
- Расхождение #3 → новый баг, не покрыт B1-B9
- Расхождение #4 → новый баг, не покрыт B1-B9
- Расхождение #5 → новый баг, не покрыт B1-B9
- Расхождение #6 → новый баг, не покрыт B1-B9

**B9 (новый): Библиотека читает toc.json и запрашивает сервер без необходимости**
- Корень: `loadLocalLibrary` читает `structure/toc.json` для каждого проекта; `loadLibrary` параллельно запрашивает `get_user_books_with_counts`
- Исправление: библиотека читает ТОЛЬКО `project.json`; серверный запрос убрать из дефолтного потока
- Файл: `src/hooks/useBookManager.ts`
